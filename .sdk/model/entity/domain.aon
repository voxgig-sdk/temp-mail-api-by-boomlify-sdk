# Entity: domain

main: kit: entity: domain: {

  alias: field: {}

  fields: [
    {
      name: "domains"
      req: false
      type: "`$ARRAY`"
    }
  ]

  name: "domain"
  op: {
    load: {
      name: "load"
      points: [
        {
          method: "GET"
          orig: "/domains"
          parts: [
            "domains"
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.data`"
          }
        }
      ]
    }
  }



}
